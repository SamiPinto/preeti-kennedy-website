PREETI KENNEDY - FOUNDER WEBSITE
Handover pack. Built 23 July 2026.
===================================================================

WHAT THIS IS
A complete, static website. 51 files, 3.5 MB. No build step, no
database, no server-side code, no dependencies to install.
It is plain HTML/CSS/JavaScript, so it will run on any web host.

-------------------------------------------------------------------
HOW TO PUT IT LIVE (pick ONE)
-------------------------------------------------------------------

OPTION A - Existing host / cPanel (most likely)
  1. Upload EVERYTHING in this folder into the site's web root
     (usually public_html/ or www/).
  2. Keep the folder structure exactly as-is. Do not move files
     out of images/, insights/, press-assets/ or uploads/.
  3. Done. index.html is the home page.

OPTION B - Netlify / Vercel / Cloudflare Pages (free, fastest)
  1. Log in, choose "deploy manually" / drag-and-drop.
  2. Drag this whole folder in.
  3. Point the domain at it. Done.

OPTION C - WordPress site already on the domain
  This is a standalone site, not a WordPress theme. Either
  (a) put it on a subdomain (e.g. preetikennedy.com), or
  (b) upload to a subfolder and link to it.
  Do NOT try to paste it into a page builder - the layout and
  scroll animations will break.

-------------------------------------------------------------------
BEFORE GOING LIVE - PLEASE CHECK
-------------------------------------------------------------------
1. Serve over HTTPS (any modern host does this by default).
2. Fonts load from Google Fonts, so the server needs normal
   outbound internet access. Nothing to configure.
3. Test on a real phone as well as desktop. The site is built
   mobile-first and was verified at 375px, 390px and 1440px:
   no sideways scrolling, all tap targets 40px+.

-------------------------------------------------------------------
THE NEWSLETTER SECTION IS HIDDEN
-------------------------------------------------------------------
The "Monthly Note" signup was never connected to an email provider,
so it validated the form and showed a success message while storing
nothing. Rather than mislead visitors, the section is now commented
out in index.html.

To bring it back once you have an email provider:
  1. Open index.html and search for:   HIDDEN
  2. Delete the comment wrapper (the <!-- ... --> around the section).
  3. Search for:   var NEWSLETTER_ENDPOINT = '';
     and put your form URL between the quotes (Mailchimp, Kit/
     ConvertKit, Formspree, Campaign Monitor - any will give you one).
     It POSTs JSON: { name, email }.

The styles and the submit handler are both still in place, so those
two steps are all it takes.

-------------------------------------------------------------------
FILE MAP
-------------------------------------------------------------------
  index.html              The entire home page (all sections + styles + scripts)
  insights/               20 article pages, one per Founder Insight essay
  images/insights/        The 20 magazine-style article covers
  images/                 Book cover
  uploads/                Hero photograph
  press-assets/logos/     8 publication logos (Featured In section)
  book/                   The ebook PDF served by "The Book" button

-------------------------------------------------------------------
KNOWN GAPS (content, not bugs)
-------------------------------------------------------------------
- The American Reporter has no logo file, so that press card shows
  the publication name in type instead. Drop
  press-assets/logos/the-american-reporter.png in to fix.
- No Filter Club and Brand Vantage Academy show "Coming soon"
  instead of a website link, because those sites do not exist yet.
- (resolved) The book button now serves the ebook directly:
  book/ambition-without-apology.pdf

Questions: Chad Jacob, Shopa Marketing.
