{
  "name": "preeti-kennedy-website",
  "version": "1.0.0",
  "private": true,
  "description": "Preeti Kennedy \u2014 founder site. Static, built with Eleventy.",
  "scripts": {
    "build": "rm -rf _site && eleventy",
    "serve": "eleventy --serve --port=8731",
    "clean": "rm -rf _site"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^6.0.0"
  }
}
